---
import { useEffect, useState } from 'react';

// Esta función se ejecutará en el lado del cliente
if (typeof window !== 'undefined') {
  const searchParams = new URLSearchParams(window.location.search);
  const gazeeventsData = searchParams.get('gazeevents');
  var gazeevents = gazeeventsData ? JSON.parse(decodeURIComponent(gazeeventsData)) : [];
}
---

<html>
  <head>
    <title>Revisión de Gazeevents</title>
  </head>
  <body>
    <h1>Revisión de Gazeevents</h1>
    <ul>
      {gazeevents.map((event, index) => (
        <li key={index}>
          X: {event.docX}, Y: {event.docY}, Estado: {event.state}
        </li>
      ))}
    </ul>
  </body>
</html>
