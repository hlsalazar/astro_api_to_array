---
const url = new URL(Astro.request.url);
const sum = url.searchParams.get('sum') || 0; // Valor por defecto 0 si el parámetro no existe
---
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Astro App</title>
</head>
<body>
    <h1>Sum: {sum}</h1>
</body>
</html>
